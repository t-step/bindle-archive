root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2

[*.md]
# Markdown here uses normal prose wrapping, not trailing-space hard breaks,
# so trimming trailing whitespace is safe.
