# Dependency notes

`left-pad-gpl` (2.3.0) is licensed GPL-3.0. This is not reflected anywhere in
`package.json` itself — the license is only documented here, in this sibling
file, which is the scenario this fixture is meant to exercise.
