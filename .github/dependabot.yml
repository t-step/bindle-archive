version: 2
updates:
  # The only external dependency surface in this repo is GitHub Actions used by
  # the CI and release workflows. Keep those action versions current.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
